name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm install

      - name: 🚀 Export Web
        env:
          EXPO_PUBLIC_BASE_URL: /MealPlex
        run: npx expo export --platform web --output-dir dist

      - name: 🛠️ Fix Asset Paths (Fallback)
        run: |
          find dist -name "*.html" -exec sed -i 's|src="/|src="/MealPlex/|g' {} +
          find dist -name "*.html" -exec sed -i 's|href="/|href="/MealPlex/|g' {} +
          find dist -name "*.html" -exec sed -i 's|url("/|url("/MealPlex/|g' {} +

      - name: 📂 Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist
